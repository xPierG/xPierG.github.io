<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}{% if pagination.pageNumber > 0 %} - Pagina {{ pagination.pageNumber + 1 }}{% endif %} | pierg.com</title>

    {% if pagination and pagination.pageNumber > 0 %}
    <meta name="robots" content="noindex, follow">
    {% endif %}

    <link rel="canonical" href="https://pierg.com{{ page.url }}">
    <link rel="stylesheet" href="/css/style.css">
    </head>
<body class="{{ bodyClass | default('layout-default') }}">
    <header>
        <a href="/" class="logo">pierg.com</a>
        <nav>
            <a href="/about/">About</a>
            <a href="/blog/">Blog</a>
            <a href="/contact/">Contact</a>
        </nav>
    </header>

    <main>
        {{ content | safe }}
    </main>

    <footer>
        <span>&copy; 2025 pierg.com - Piergiorgio Grossi</span>

        {# Questo è un blocco condizionale di Nunjucks (il linguaggio di templating di Eleventy) #}
        {%- if page.url == '/' -%}
            <span class="photo-credit-footer">
                <a href="https://silviacasali.it/" target="_blank" rel="noopener noreferrer">PierG pic by Silvia Casali</a>
            </span>
        {%- endif -%}
    </footer>

<script src="/js/contact-form.js"></script>
</body>
</html>
